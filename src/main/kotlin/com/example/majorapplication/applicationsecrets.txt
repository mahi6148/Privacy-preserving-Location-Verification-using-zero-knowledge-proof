postgres docker run steps:
~docker volume create pgdata
~docker run --name postgres-majorapplication-database -v pgdatamajorapplicationbackend:/var/lib/postgresql/data -e POSTGRES_PASSWORD=majorapplication@123 -e POSTGRES_USER=majorapplicationuser -e POSTGRES_DB=majorapplicationbackend -p 5432:5432 -d postgres

~docker exec -it postgres-majorapplication-database psql -U majorapplicationuser -d majorapplicationbackend

-- 1. Users Table
CREATE TABLE Users (
    user_id UUID PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    email VARCHAR(255) NOT NULL UNIQUE,
    registered_on TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
);

-- 2. Attendance Records Table
CREATE TABLE AttendanceRecords (
    record_id SERIAL PRIMARY KEY,
    user_id UUID NOT NULL,
    timestamp TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(10) NOT NULL CHECK (status IN ('PRESENT', 'ABSENT', 'ERROR')),
    session_id INTEGER,
    FOREIGN KEY (user_id) REFERENCES Users(user_id),
    FOREIGN KEY (session_id) REFERENCES Sessions(session_id)
);

-- 3. Sessions Table (Optional)
CREATE TABLE Sessions (
    session_id SERIAL PRIMARY KEY,
    session_name VARCHAR(255) NOT NULL,
    start_time TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    end_time TIMESTAMP WITHOUT TIME ZONE,
    location_limit NUMERIC
);

-- User-Session association table
CREATE TABLE UserSessions (
    user_id UUID NOT NULL,
    session_id INTEGER NOT NULL,
    registration_date TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (user_id, session_id),
    FOREIGN KEY (user_id) REFERENCES Users(user_id),
    FOREIGN KEY (session_id) REFERENCES Sessions(session_id)
);



GRPC article : https://themaroonknight.medium.com/grpc-set-up-with-spring-boot-v3-0-1-and-kotlin-70aa56e9598b
